# Configuración del Perceptrón Multicapa (MLP)
# Archivo de configuración para entrenamiento

# Hiperparámetros de entrenamiento
epochs=100
batch_size=64
learning_rate=0.01
lr_decay=0.98

# Optimizador
momentum=0.9
weight_decay=0.0001

# Early stopping
early_stopping_patience=15
min_delta=0.001

# Arquitectura (capas ocultas)
# Formato: neurona1,neurona2,...
hidden_layers=20,10

# Seed para reproducibilidad
seed=42

# Verbosity (0=silencioso, 1=normal, 2=detallado)
verbose=1
